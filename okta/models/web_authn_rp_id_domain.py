# The Okta software accompanied by this notice is provided pursuant to the following terms:
# Copyright © 2025-Present, Okta, Inc.
# Licensed under the Apache License, Version 2.0 (the "License"); you may not use this file except in compliance with the
# License.
# You may obtain a copy of the License at http://www.apache.org/licenses/LICENSE-2.0.
# Unless required by applicable law or agreed to in writing, software distributed under the License is distributed on an "AS
# IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and limitations under the License.
# coding: utf-8

"""
Okta Admin Management

Allows customers to easily access the Okta Management APIs

The version of the OpenAPI document: 5.1.0
Contact: devex-public@okta.com
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""  # noqa: E501

from __future__ import annotations

import json
import pprint
import re  # noqa: F401
from typing import Any, ClassVar, Dict, List
from typing import Optional, Set

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing_extensions import Self

from okta.models.web_authn_rp_id_domain_dns_record import WebAuthnRpIdDomainDnsRecord


class WebAuthnRpIdDomain(BaseModel):
    """
    The RP domain object for the WebAuthn configuration
    """  # noqa: E501

    dns_record: Optional[WebAuthnRpIdDomainDnsRecord] = Field(
        default=None, alias="dnsRecord"
    )
    name: Optional[StrictStr] = Field(
        default=None,
        description="The [RP ID](https://www.w3.org/TR/webauthn/#relying-party-identifier) domain value to be used for all "
        "WebAuthn operations.  If it isn't specified, the `domain` object isn't included in the request, "
        "and the domain value defaults to the domain of the current page (the domain of your org or a custom "
        "domain, for example).  > **Note:** If you don't use a custom RP ID (the default behavior), "
        "the domain value defaults to the end user's current page. The domain value defaults to the full domain "
        "name of the page that the end user is on when they're attempting the WebAuthn credential operation ("
        "enrollment or verification).",
    )
    validation_status: Optional[StrictStr] = Field(
        default=None,
        description="Indicates the validation status of the domain",
        alias="validationStatus",
    )
    __properties: ClassVar[List[str]] = ["dnsRecord", "name", "validationStatus"]

    @field_validator("validation_status")
    def validation_status_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(["NOT_STARTED", "IN_PROGRESS", "VERIFIED", "ERROR"]):
            raise ValueError(
                "must be one of enum values ('NOT_STARTED', 'IN_PROGRESS', 'VERIFIED', 'ERROR')"
            )
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of WebAuthnRpIdDomain from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * OpenAPI `readOnly` fields are excluded.
        """
        excluded_fields: Set[str] = set(
            [
                "validation_status",
            ]
        )

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of dns_record
        if self.dns_record:
            if not isinstance(self.dns_record, dict):
                _dict["dnsRecord"] = self.dns_record.to_dict()
            else:
                _dict["dnsRecord"] = self.dns_record

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of WebAuthnRpIdDomain from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate(
            {
                "dnsRecord": (
                    WebAuthnRpIdDomainDnsRecord.from_dict(obj["dnsRecord"])
                    if obj.get("dnsRecord") is not None
                    else None
                ),
                "name": obj.get("name"),
                "validationStatus": obj.get("validationStatus"),
            }
        )
        return _obj
