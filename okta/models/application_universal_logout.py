# The Okta software accompanied by this notice is provided pursuant to the following terms:
# Copyright © 2025-Present, Okta, Inc.
# Licensed under the Apache License, Version 2.0 (the "License"); you may not use this file except in compliance with the
# License.
# You may obtain a copy of the License at http://www.apache.org/licenses/LICENSE-2.0.
# Unless required by applicable law or agreed to in writing, software distributed under the License is distributed on an "AS
# IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and limitations under the License.
# coding: utf-8

"""
Okta Admin Management

Allows customers to easily access the Okta Management APIs

The version of the OpenAPI document: 5.1.0
Contact: devex-public@okta.com
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""  # noqa: E501

from __future__ import annotations

import json
import pprint
import re  # noqa: F401
from typing import Any, ClassVar, Dict, List
from typing import Optional, Set

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing_extensions import Self


class ApplicationUniversalLogout(BaseModel):
    """
    <div class=\"x-lifecycle-container\"><x-lifecycle class=\"oie\"></x-lifecycle></div> Universal Logout properties for the
    app. These properties are only returned and can't be updated.
    """  # noqa: E501

    identity_stack: Optional[StrictStr] = Field(
        default=None,
        description="Indicates whether the app uses a shared identity stack that may cause the user to sign out of other "
        "apps by the same company",
        alias="identityStack",
    )
    protocol: Optional[StrictStr] = Field(
        default=None, description="The protocol used for Universal Logout"
    )
    status: Optional[StrictStr] = Field(
        default=None, description="Universal Logout status for the app instance"
    )
    support_type: Optional[StrictStr] = Field(
        default=None,
        description="Indicates whether the app supports full or partial Universal Logout (UL).",
        alias="supportType",
    )
    __properties: ClassVar[List[str]] = [
        "identityStack",
        "protocol",
        "status",
        "supportType",
    ]

    @field_validator("identity_stack")
    def identity_stack_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(["SHARED", "NOT_SHARED"]):
            raise ValueError("must be one of enum values ('SHARED', 'NOT_SHARED')")
        return value

    @field_validator("protocol")
    def protocol_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(["PROPRIETARY", "GLOBAL_TOKEN_REVOCATION"]):
            raise ValueError(
                "must be one of enum values ('PROPRIETARY', 'GLOBAL_TOKEN_REVOCATION')"
            )
        return value

    @field_validator("status")
    def status_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(["ENABLED", "DISABLED", "UNSUPPORTED"]):
            raise ValueError(
                "must be one of enum values ('ENABLED', 'DISABLED', 'UNSUPPORTED')"
            )
        return value

    @field_validator("support_type")
    def support_type_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(["FULL", "PARTIAL"]):
            raise ValueError("must be one of enum values ('FULL', 'PARTIAL')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ApplicationUniversalLogout from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ApplicationUniversalLogout from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate(
            {
                "identityStack": obj.get("identityStack"),
                "protocol": obj.get("protocol"),
                "status": obj.get("status"),
                "supportType": obj.get("supportType"),
            }
        )
        return _obj
