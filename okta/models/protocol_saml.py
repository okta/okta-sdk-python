# The Okta software accompanied by this notice is provided pursuant to the following terms:
# Copyright © 2025-Present, Okta, Inc.
# Licensed under the Apache License, Version 2.0 (the "License"); you may not use this file except in compliance with the
# License.
# You may obtain a copy of the License at http://www.apache.org/licenses/LICENSE-2.0.
# Unless required by applicable law or agreed to in writing, software distributed under the License is distributed on an "AS
# IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and limitations under the License.
# coding: utf-8

"""
Okta Admin Management

Allows customers to easily access the Okta Management APIs

The version of the OpenAPI document: 5.1.0
Contact: devex-public@okta.com
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""  # noqa: E501

from __future__ import annotations

import json
import pprint
import re  # noqa: F401
from typing import Any, ClassVar, Dict, List
from typing import Optional, Set

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing_extensions import Self

from okta.models.saml_algorithms import SamlAlgorithms
from okta.models.saml_credentials import SamlCredentials
from okta.models.saml_endpoints import SamlEndpoints
from okta.models.saml_relay_state import SamlRelayState
from okta.models.saml_settings import SamlSettings


class ProtocolSaml(BaseModel):
    """
    Protocol settings for the [SAML 2.0 Authentication Request Protocol](
    http://docs.oasis-open.org/security/saml/v2.0/saml-core-2.0-os.pdf)
    """  # noqa: E501

    algorithms: Optional[SamlAlgorithms] = None
    credentials: Optional[SamlCredentials] = None
    endpoints: Optional[SamlEndpoints] = None
    relay_state: Optional[SamlRelayState] = Field(default=None, alias="relayState")
    settings: Optional[SamlSettings] = None
    type: Optional[StrictStr] = Field(default=None, description="SAML 2.0 protocol")
    __properties: ClassVar[List[str]] = [
        "algorithms",
        "credentials",
        "endpoints",
        "relayState",
        "settings",
        "type",
    ]

    @field_validator("type")
    def type_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(["SAML2"]):
            raise ValueError("must be one of enum values ('SAML2')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ProtocolSaml from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of algorithms
        if self.algorithms:
            if not isinstance(self.algorithms, dict):
                _dict["algorithms"] = self.algorithms.to_dict()
            else:
                _dict["algorithms"] = self.algorithms

        # override the default output from pydantic by calling `to_dict()` of credentials
        if self.credentials:
            if not isinstance(self.credentials, dict):
                _dict["credentials"] = self.credentials.to_dict()
            else:
                _dict["credentials"] = self.credentials

        # override the default output from pydantic by calling `to_dict()` of endpoints
        if self.endpoints:
            if not isinstance(self.endpoints, dict):
                _dict["endpoints"] = self.endpoints.to_dict()
            else:
                _dict["endpoints"] = self.endpoints

        # override the default output from pydantic by calling `to_dict()` of relay_state
        if self.relay_state:
            if not isinstance(self.relay_state, dict):
                _dict["relayState"] = self.relay_state.to_dict()
            else:
                _dict["relayState"] = self.relay_state

        # override the default output from pydantic by calling `to_dict()` of settings
        if self.settings:
            if not isinstance(self.settings, dict):
                _dict["settings"] = self.settings.to_dict()
            else:
                _dict["settings"] = self.settings

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ProtocolSaml from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate(
            {
                "algorithms": (
                    SamlAlgorithms.from_dict(obj["algorithms"])
                    if obj.get("algorithms") is not None
                    else None
                ),
                "credentials": (
                    SamlCredentials.from_dict(obj["credentials"])
                    if obj.get("credentials") is not None
                    else None
                ),
                "endpoints": (
                    SamlEndpoints.from_dict(obj["endpoints"])
                    if obj.get("endpoints") is not None
                    else None
                ),
                "relayState": (
                    SamlRelayState.from_dict(obj["relayState"])
                    if obj.get("relayState") is not None
                    else None
                ),
                "settings": (
                    SamlSettings.from_dict(obj["settings"])
                    if obj.get("settings") is not None
                    else None
                ),
                "type": obj.get("type"),
            }
        )
        return _obj
