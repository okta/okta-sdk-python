# The Okta software accompanied by this notice is provided pursuant to the following terms:
# Copyright © 2025-Present, Okta, Inc.
# Licensed under the Apache License, Version 2.0 (the "License"); you may not use this file except in compliance with the
# License.
# You may obtain a copy of the License at http://www.apache.org/licenses/LICENSE-2.0.
# Unless required by applicable law or agreed to in writing, software distributed under the License is distributed on an "AS
# IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and limitations under the License.
# coding: utf-8

"""
Okta Admin Management

Allows customers to easily access the Okta Management APIs

The version of the OpenAPI document: 5.1.0
Contact: devex-public@okta.com
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""  # noqa: E501

from __future__ import annotations

import json
import pprint
import re  # noqa: F401
from datetime import datetime
from importlib import import_module
from typing import Any, ClassVar, Dict, List, Union
from typing import Optional, Set
from typing import TYPE_CHECKING

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing_extensions import Annotated

from okta.models.service_account_status import ServiceAccountStatus
from okta.models.service_account_status_detail import ServiceAccountStatusDetail
from okta.models.service_account_type import ServiceAccountType

if TYPE_CHECKING:
    from okta.models.service_account_details_app_account import (
        ServiceAccountDetailsAppAccount,
    )
    from okta.models.service_account_details_okta_user_account import (
        ServiceAccountDetailsOktaUserAccount,
    )


class ServiceAccount(BaseModel):
    """
    ServiceAccount
    """  # noqa: E501

    account_type: ServiceAccountType = Field(alias="accountType")
    created: Optional[datetime] = Field(
        default=None, description="Timestamp when the service account was created"
    )
    description: Optional[
        Annotated[str, Field(min_length=0, strict=True, max_length=255)]
    ] = Field(default=None, description="The description of the service account")
    id: Optional[Annotated[str, Field(strict=True)]] = Field(
        default=None, description="The UUID of the service account"
    )
    last_updated: Optional[datetime] = Field(
        default=None,
        description="Timestamp when the service account was last updated",
        alias="lastUpdated",
    )
    name: Annotated[str, Field(min_length=1, strict=True, max_length=50)] = Field(
        description="The user-defined name for the service account"
    )
    owner_group_ids: Optional[
        Annotated[List[StrictStr], Field(min_length=0, max_length=10)]
    ] = Field(
        default=None,
        description="A list of IDs of the Okta groups that own the service account",
        alias="ownerGroupIds",
    )
    owner_user_ids: Optional[
        Annotated[List[StrictStr], Field(min_length=0, max_length=10)]
    ] = Field(
        default=None,
        description="A list of IDs of the Okta users that own the service account",
        alias="ownerUserIds",
    )
    status: Optional[ServiceAccountStatus] = None
    status_detail: Optional[ServiceAccountStatusDetail] = Field(
        default=None, alias="statusDetail"
    )
    __properties: ClassVar[List[str]] = [
        "accountType",
        "created",
        "description",
        "id",
        "lastUpdated",
        "name",
        "ownerGroupIds",
        "ownerUserIds",
        "status",
        "statusDetail",
    ]

    @field_validator("id")
    def id_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not re.match(
            r"(?i)^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$",
            value,
        ):
            raise ValueError(
                r"must validate the regular expression /(?i)^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-["
                r"0-9a-f]{12}$/"
            )
        return value

    @field_validator("name")
    def name_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if not re.match(r"^[\w\-_. ]+$", value):
            raise ValueError(r"must validate the regular expression /^[\w\-_. ]+$/")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )

    # JSON field name that stores the object type
    __discriminator_property_name: ClassVar[str] = "accountType"

    # discriminator mappings
    __discriminator_value_class_map: ClassVar[Dict[str, str]] = {
        "APP_ACCOUNT": "ServiceAccountDetailsAppAccount",
        "OKTA_USER_ACCOUNT": "ServiceAccountDetailsOktaUserAccount",
    }

    @classmethod
    def get_discriminator_value(cls, obj: Dict[str, Any]) -> Optional[str]:
        """Returns the discriminator value (object type) of the data"""
        discriminator_value = obj[cls.__discriminator_property_name]
        if discriminator_value:
            return cls.__discriminator_value_class_map.get(discriminator_value)
        else:
            return None

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(
        cls, json_str: str
    ) -> Optional[
        Union[ServiceAccountDetailsAppAccount, ServiceAccountDetailsOktaUserAccount]
    ]:
        """Create an instance of ServiceAccount from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * OpenAPI `readOnly` fields are excluded.
        * OpenAPI `readOnly` fields are excluded.
        * OpenAPI `readOnly` fields are excluded.
        """
        excluded_fields: Set[str] = set(
            [
                "created",
                "id",
                "last_updated",
            ]
        )

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(
        cls, obj: Dict[str, Any]
    ) -> Optional[
        Union[ServiceAccountDetailsAppAccount, ServiceAccountDetailsOktaUserAccount]
    ]:
        """Create an instance of ServiceAccount from a dict"""
        # look up the object type based on discriminator mapping
        object_type = cls.get_discriminator_value(obj)
        if object_type == "ServiceAccountDetailsAppAccount":
            return import_module(
                "okta.models.service_account_details_app_account"
            ).ServiceAccountDetailsAppAccount.from_dict(obj)
        if object_type == "ServiceAccountDetailsOktaUserAccount":
            return import_module(
                "okta.models.service_account_details_okta_user_account"
            ).ServiceAccountDetailsOktaUserAccount.from_dict(obj)

        raise ValueError(
            "ServiceAccount failed to lookup discriminator value from "
            + json.dumps(obj)
            + ". Discriminator property name: "
            + cls.__discriminator_property_name
            + ", mapping: "
            + json.dumps(cls.__discriminator_value_class_map)
        )
