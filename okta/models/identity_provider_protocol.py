# The Okta software accompanied by this notice is provided pursuant to the following terms:
# Copyright © 2025-Present, Okta, Inc.
# Licensed under the Apache License, Version 2.0 (the "License"); you may not use this file except in compliance with the
# License.
# You may obtain a copy of the License at http://www.apache.org/licenses/LICENSE-2.0.
# Unless required by applicable law or agreed to in writing, software distributed under the License is distributed on an "AS
# IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and limitations under the License.
# coding: utf-8

"""
Okta Admin Management

Allows customers to easily access the Okta Management APIs

The version of the OpenAPI document: 5.1.0
Contact: devex-public@okta.com
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""  # noqa: E501

from __future__ import annotations

import json
import pprint
from typing import Any
from typing import Union, Set, Optional, Dict

from pydantic import (
    BaseModel,
    ConfigDict,
    ValidationError,
    field_validator,
)
from typing_extensions import Self

from okta.models.protocol_id_verification import ProtocolIdVerification
from okta.models.protocol_mtls import ProtocolMtls
from okta.models.protocol_o_auth import ProtocolOAuth
from okta.models.protocol_oidc import ProtocolOidc
from okta.models.protocol_saml import ProtocolSaml

IDENTITYPROVIDERPROTOCOL_ONE_OF_SCHEMAS = [
    "ProtocolIdVerification",
    "ProtocolMtls",
    "ProtocolOAuth",
    "ProtocolOidc",
    "ProtocolSaml",
]


class IdentityProviderProtocol(BaseModel):
    """
    IdP-specific protocol settings for endpoints, bindings, and algorithms used to connect with the IdP and validate messages
    """

    # data type: ProtocolSaml
    oneof_schema_1_validator: Optional[ProtocolSaml] = None
    # data type: ProtocolOAuth
    oneof_schema_2_validator: Optional[ProtocolOAuth] = None
    # data type: ProtocolOidc
    oneof_schema_3_validator: Optional[ProtocolOidc] = None
    # data type: ProtocolMtls
    oneof_schema_4_validator: Optional[ProtocolMtls] = None
    # data type: ProtocolIdVerification
    oneof_schema_5_validator: Optional[ProtocolIdVerification] = None
    actual_instance: Optional[
        Union[
            ProtocolIdVerification,
            ProtocolMtls,
            ProtocolOAuth,
            ProtocolOidc,
            ProtocolSaml,
        ]
    ] = None
    one_of_schemas: Set[str] = {
        "ProtocolIdVerification",
        "ProtocolMtls",
        "ProtocolOAuth",
        "ProtocolOidc",
        "ProtocolSaml",
    }

    model_config = ConfigDict(
        validate_assignment=True,
        protected_namespaces=(),
    )

    def __init__(self, *args, **kwargs) -> None:
        if args:
            if len(args) > 1:
                raise ValueError(
                    "If a position argument is used, only 1 is allowed to set `actual_instance`"
                )
            if kwargs:
                raise ValueError(
                    "If a position argument is used, keyword arguments cannot be used."
                )
            super().__init__(actual_instance=args[0])
        else:
            super().__init__(**kwargs)

    @field_validator("actual_instance")
    def actual_instance_must_validate_oneof(cls, v):
        error_messages = []
        match = 0
        # validate data type: ProtocolSaml
        if not isinstance(v, ProtocolSaml):
            error_messages.append(
                f"Error! Input type `{type(v)}` is not `ProtocolSaml`"
            )
        else:
            match += 1
        # validate data type: ProtocolOAuth
        if not isinstance(v, ProtocolOAuth):
            error_messages.append(
                f"Error! Input type `{type(v)}` is not `ProtocolOAuth`"
            )
        else:
            match += 1
        # validate data type: ProtocolOidc
        if not isinstance(v, ProtocolOidc):
            error_messages.append(
                f"Error! Input type `{type(v)}` is not `ProtocolOidc`"
            )
        else:
            match += 1
        # validate data type: ProtocolMtls
        if not isinstance(v, ProtocolMtls):
            error_messages.append(
                f"Error! Input type `{type(v)}` is not `ProtocolMtls`"
            )
        else:
            match += 1
        # validate data type: ProtocolIdVerification
        if not isinstance(v, ProtocolIdVerification):
            error_messages.append(
                f"Error! Input type `{type(v)}` is not `ProtocolIdVerification`"
            )
        else:
            match += 1
        if match > 1:
            # more than 1 match
            raise ValueError(
                "Multiple matches found when setting `actual_instance` in IdentityProviderProtocol with oneOf schemas: "
                "ProtocolIdVerification, ProtocolMtls, ProtocolOAuth, ProtocolOidc, ProtocolSaml. Details: "
                + ", ".join(error_messages)
            )
        elif match == 0:
            # no match
            raise ValueError(
                "No match found when setting `actual_instance` in IdentityProviderProtocol with oneOf schemas: "
                "ProtocolIdVerification, ProtocolMtls, ProtocolOAuth, ProtocolOidc, ProtocolSaml. Details: "
                + ", ".join(error_messages)
            )
        else:
            return v

    @classmethod
    def from_dict(cls, obj: Union[str, Dict[str, Any]]) -> Self:
        return cls.from_json(json.dumps(obj))

    @classmethod
    def from_json(cls, json_str: str) -> Self:
        """Returns the object represented by the json string"""
        instance = cls.model_construct()
        error_messages = []
        match = 0

        # deserialize data into ProtocolSaml
        try:
            instance.actual_instance = ProtocolSaml.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into ProtocolOAuth
        try:
            instance.actual_instance = ProtocolOAuth.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into ProtocolOidc
        try:
            instance.actual_instance = ProtocolOidc.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into ProtocolMtls
        try:
            instance.actual_instance = ProtocolMtls.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into ProtocolIdVerification
        try:
            instance.actual_instance = ProtocolIdVerification.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))

        if match > 1:
            # more than 1 match
            raise ValueError(
                "Multiple matches found when deserializing the JSON string into IdentityProviderProtocol with oneOf schemas: "
                "ProtocolIdVerification, ProtocolMtls, ProtocolOAuth, ProtocolOidc, ProtocolSaml. Details: "
                + ", ".join(error_messages)
            )
        elif match == 0:
            # no match
            raise ValueError(
                "No match found when deserializing the JSON string into IdentityProviderProtocol with oneOf schemas: "
                "ProtocolIdVerification, ProtocolMtls, ProtocolOAuth, ProtocolOidc, ProtocolSaml. Details: "
                + ", ".join(error_messages)
            )
        else:
            return instance

    def to_json(self) -> str:
        """Returns the JSON representation of the actual instance"""
        if self.actual_instance is None:
            return "null"

        if hasattr(self.actual_instance, "to_json") and callable(
            self.actual_instance.to_json
        ):
            return self.actual_instance.to_json()
        else:
            return json.dumps(self.actual_instance)

    def to_dict(
        self,
    ) -> Optional[
        Union[
            Dict[str, Any],
            ProtocolIdVerification,
            ProtocolMtls,
            ProtocolOAuth,
            ProtocolOidc,
            ProtocolSaml,
        ]
    ]:
        """Returns the dict representation of the actual instance"""
        if self.actual_instance is None:
            return None

        if hasattr(self.actual_instance, "to_dict") and callable(
            self.actual_instance.to_dict
        ):
            return self.actual_instance.to_dict()
        else:
            # primitive type
            return self.actual_instance

    def to_str(self) -> str:
        """Returns the string representation of the actual instance"""
        return pprint.pformat(self.model_dump())
