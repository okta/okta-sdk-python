# The Okta software accompanied by this notice is provided pursuant to the following terms:
# Copyright © 2025-Present, Okta, Inc.
# Licensed under the Apache License, Version 2.0 (the "License"); you may not use this file except in compliance with the
# License.
# You may obtain a copy of the License at http://www.apache.org/licenses/LICENSE-2.0.
# Unless required by applicable law or agreed to in writing, software distributed under the License is distributed on an "AS
# IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and limitations under the License.
# coding: utf-8

"""
Okta Admin Management

Allows customers to easily access the Okta Management APIs

The version of the OpenAPI document: 5.1.0
Contact: devex-public@okta.com
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""  # noqa: E501

from __future__ import annotations

import json
import pprint
from typing import Any
from typing import Union, Set, Optional, Dict

from pydantic import (
    BaseModel,
    ConfigDict,
    ValidationError,
    field_validator,
)
from typing_extensions import Self

from okta.models.identity_assertion_app_instance_connection_creatable import (
    IdentityAssertionAppInstanceConnectionCreatable,
)
from okta.models.identity_assertion_custom_as_connection_creatable import (
    IdentityAssertionCustomASConnectionCreatable,
)
from okta.models.sts_service_account_connection_creatable import (
    STSServiceAccountConnectionCreatable,
)
from okta.models.sts_vault_secret_connection_creatable import (
    STSVaultSecretConnectionCreatable,
)

MANAGEDCONNECTIONCREATABLE_ONE_OF_SCHEMAS = [
    "IdentityAssertionAppInstanceConnectionCreatable",
    "IdentityAssertionCustomASConnectionCreatable",
    "STSServiceAccountConnectionCreatable",
    "STSVaultSecretConnectionCreatable",
]


class ManagedConnectionCreatable(BaseModel):
    """
    Create a new connection
    """

    # data type: IdentityAssertionAppInstanceConnectionCreatable
    oneof_schema_1_validator: Optional[
        IdentityAssertionAppInstanceConnectionCreatable
    ] = None
    # data type: IdentityAssertionCustomASConnectionCreatable
    oneof_schema_2_validator: Optional[IdentityAssertionCustomASConnectionCreatable] = (
        None
    )
    # data type: STSVaultSecretConnectionCreatable
    oneof_schema_3_validator: Optional[STSVaultSecretConnectionCreatable] = None
    # data type: STSServiceAccountConnectionCreatable
    oneof_schema_4_validator: Optional[STSServiceAccountConnectionCreatable] = None
    actual_instance: Optional[
        Union[
            IdentityAssertionAppInstanceConnectionCreatable,
            IdentityAssertionCustomASConnectionCreatable,
            STSServiceAccountConnectionCreatable,
            STSVaultSecretConnectionCreatable,
        ]
    ] = None
    one_of_schemas: Set[str] = {
        "IdentityAssertionAppInstanceConnectionCreatable",
        "IdentityAssertionCustomASConnectionCreatable",
        "STSServiceAccountConnectionCreatable",
        "STSVaultSecretConnectionCreatable",
    }

    model_config = ConfigDict(
        validate_assignment=True,
        protected_namespaces=(),
    )

    discriminator_value_class_map: Dict[str, str] = {}

    def __init__(self, *args, **kwargs) -> None:
        if args:
            if len(args) > 1:
                raise ValueError(
                    "If a position argument is used, only 1 is allowed to set `actual_instance`"
                )
            if kwargs:
                raise ValueError(
                    "If a position argument is used, keyword arguments cannot be used."
                )
            super().__init__(actual_instance=args[0])
        else:
            super().__init__(**kwargs)

    @field_validator("actual_instance")
    def actual_instance_must_validate_oneof(cls, v):
        error_messages = []
        match = 0
        # validate data type: IdentityAssertionAppInstanceConnectionCreatable
        if not isinstance(v, IdentityAssertionAppInstanceConnectionCreatable):
            error_messages.append(
                f"Error! Input type `{type(v)}` is not `IdentityAssertionAppInstanceConnectionCreatable`"
            )
        else:
            match += 1
        # validate data type: IdentityAssertionCustomASConnectionCreatable
        if not isinstance(v, IdentityAssertionCustomASConnectionCreatable):
            error_messages.append(
                f"Error! Input type `{type(v)}` is not `IdentityAssertionCustomASConnectionCreatable`"
            )
        else:
            match += 1
        # validate data type: STSVaultSecretConnectionCreatable
        if not isinstance(v, STSVaultSecretConnectionCreatable):
            error_messages.append(
                f"Error! Input type `{type(v)}` is not `STSVaultSecretConnectionCreatable`"
            )
        else:
            match += 1
        # validate data type: STSServiceAccountConnectionCreatable
        if not isinstance(v, STSServiceAccountConnectionCreatable):
            error_messages.append(
                f"Error! Input type `{type(v)}` is not `STSServiceAccountConnectionCreatable`"
            )
        else:
            match += 1
        if match > 1:
            # more than 1 match
            raise ValueError(
                "Multiple matches found when setting `actual_instance` in ManagedConnectionCreatable with oneOf schemas: "
                "IdentityAssertionAppInstanceConnectionCreatable, IdentityAssertionCustomASConnectionCreatable, "
                "STSServiceAccountConnectionCreatable, STSVaultSecretConnectionCreatable. Details: "
                + ", ".join(error_messages)
            )
        elif match == 0:
            # no match
            raise ValueError(
                "No match found when setting `actual_instance` in ManagedConnectionCreatable with oneOf schemas: "
                "IdentityAssertionAppInstanceConnectionCreatable, IdentityAssertionCustomASConnectionCreatable, "
                "STSServiceAccountConnectionCreatable, STSVaultSecretConnectionCreatable. Details: "
                + ", ".join(error_messages)
            )
        else:
            return v

    @classmethod
    def from_dict(cls, obj: Union[str, Dict[str, Any]]) -> Self:
        return cls.from_json(json.dumps(obj))

    @classmethod
    def from_json(cls, json_str: str) -> Self:
        """Returns the object represented by the json string"""
        instance = cls.model_construct()
        error_messages = []
        match = 0

        # use oneOf discriminator to lookup the data type
        _data_type = json.loads(json_str).get("connectionType")
        if not _data_type:
            raise ValueError(
                "Failed to lookup data type from the field `connectionType` in the input."
            )

        # check if data type is `IdentityAssertionAppInstanceConnectionCreatable`
        if _data_type == "IDENTITY_ASSERTION_APP_INSTANCE":
            instance.actual_instance = (
                IdentityAssertionAppInstanceConnectionCreatable.from_json(json_str)
            )
            return instance

        # check if data type is `IdentityAssertionCustomASConnectionCreatable`
        if _data_type == "IDENTITY_ASSERTION_CUSTOM_AS":
            instance.actual_instance = (
                IdentityAssertionCustomASConnectionCreatable.from_json(json_str)
            )
            return instance

        # check if data type is `STSServiceAccountConnectionCreatable`
        if _data_type == "STS_SERVICE_ACCOUNT":
            instance.actual_instance = STSServiceAccountConnectionCreatable.from_json(
                json_str
            )
            return instance

        # check if data type is `STSVaultSecretConnectionCreatable`
        if _data_type == "STS_VAULT_SECRET":
            instance.actual_instance = STSVaultSecretConnectionCreatable.from_json(
                json_str
            )
            return instance

        # check if data type is `IdentityAssertionAppInstanceConnectionCreatable`
        if _data_type == "IdentityAssertionAppInstanceConnectionCreatable":
            instance.actual_instance = (
                IdentityAssertionAppInstanceConnectionCreatable.from_json(json_str)
            )
            return instance

        # check if data type is `IdentityAssertionCustomASConnectionCreatable`
        if _data_type == "IdentityAssertionCustomASConnectionCreatable":
            instance.actual_instance = (
                IdentityAssertionCustomASConnectionCreatable.from_json(json_str)
            )
            return instance

        # check if data type is `STSServiceAccountConnectionCreatable`
        if _data_type == "STSServiceAccountConnectionCreatable":
            instance.actual_instance = STSServiceAccountConnectionCreatable.from_json(
                json_str
            )
            return instance

        # check if data type is `STSVaultSecretConnectionCreatable`
        if _data_type == "STSVaultSecretConnectionCreatable":
            instance.actual_instance = STSVaultSecretConnectionCreatable.from_json(
                json_str
            )
            return instance

        # deserialize data into IdentityAssertionAppInstanceConnectionCreatable
        try:
            instance.actual_instance = (
                IdentityAssertionAppInstanceConnectionCreatable.from_json(json_str)
            )
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into IdentityAssertionCustomASConnectionCreatable
        try:
            instance.actual_instance = (
                IdentityAssertionCustomASConnectionCreatable.from_json(json_str)
            )
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into STSVaultSecretConnectionCreatable
        try:
            instance.actual_instance = STSVaultSecretConnectionCreatable.from_json(
                json_str
            )
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into STSServiceAccountConnectionCreatable
        try:
            instance.actual_instance = STSServiceAccountConnectionCreatable.from_json(
                json_str
            )
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))

        if match > 1:
            # more than 1 match
            raise ValueError(
                "Multiple matches found when deserializing the JSON string into ManagedConnectionCreatable with oneOf "
                "schemas: IdentityAssertionAppInstanceConnectionCreatable, IdentityAssertionCustomASConnectionCreatable, "
                "STSServiceAccountConnectionCreatable, STSVaultSecretConnectionCreatable. Details: "
                + ", ".join(error_messages)
            )
        elif match == 0:
            # no match
            raise ValueError(
                "No match found when deserializing the JSON string into ManagedConnectionCreatable with oneOf schemas: "
                "IdentityAssertionAppInstanceConnectionCreatable, IdentityAssertionCustomASConnectionCreatable, "
                "STSServiceAccountConnectionCreatable, STSVaultSecretConnectionCreatable. Details: "
                + ", ".join(error_messages)
            )
        else:
            return instance

    def to_json(self) -> str:
        """Returns the JSON representation of the actual instance"""
        if self.actual_instance is None:
            return "null"

        if hasattr(self.actual_instance, "to_json") and callable(
            self.actual_instance.to_json
        ):
            return self.actual_instance.to_json()
        else:
            return json.dumps(self.actual_instance)

    def to_dict(
        self,
    ) -> Optional[
        Union[
            Dict[str, Any],
            IdentityAssertionAppInstanceConnectionCreatable,
            IdentityAssertionCustomASConnectionCreatable,
            STSServiceAccountConnectionCreatable,
            STSVaultSecretConnectionCreatable,
        ]
    ]:
        """Returns the dict representation of the actual instance"""
        if self.actual_instance is None:
            return None

        if hasattr(self.actual_instance, "to_dict") and callable(
            self.actual_instance.to_dict
        ):
            return self.actual_instance.to_dict()
        else:
            # primitive type
            return self.actual_instance

    def to_str(self) -> str:
        """Returns the string representation of the actual instance"""
        return pprint.pformat(self.model_dump())
