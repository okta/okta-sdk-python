# The Okta software accompanied by this notice is provided pursuant to the following terms:
# Copyright © 2025-Present, Okta, Inc.
# Licensed under the Apache License, Version 2.0 (the "License"); you may not use this file except in compliance with the
# License.
# You may obtain a copy of the License at http://www.apache.org/licenses/LICENSE-2.0.
# Unless required by applicable law or agreed to in writing, software distributed under the License is distributed on an "AS
# IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and limitations under the License.
# coding: utf-8

"""
Okta Admin Management

Allows customers to easily access the Okta Management APIs

The version of the OpenAPI document: 5.1.0
Contact: devex-public@okta.com
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""  # noqa: E501

from __future__ import annotations

import json
import pprint
from typing import Any
from typing import Union, Set, Optional, Dict

from pydantic import (
    BaseModel,
    ConfigDict,
    ValidationError,
    field_validator,
)
from typing_extensions import Self

from okta.models.app_user_credentials_request_payload import (
    AppUserCredentialsRequestPayload,
)
from okta.models.app_user_profile_request_payload import AppUserProfileRequestPayload

APPUSERUPDATEREQUEST_ONE_OF_SCHEMAS = [
    "AppUserCredentialsRequestPayload",
    "AppUserProfileRequestPayload",
]


class AppUserUpdateRequest(BaseModel):
    """
    AppUserUpdateRequest
    """

    # data type: AppUserCredentialsRequestPayload
    oneof_schema_1_validator: Optional[AppUserCredentialsRequestPayload] = None
    # data type: AppUserProfileRequestPayload
    oneof_schema_2_validator: Optional[AppUserProfileRequestPayload] = None
    actual_instance: Optional[
        Union[AppUserCredentialsRequestPayload, AppUserProfileRequestPayload]
    ] = None
    one_of_schemas: Set[str] = {
        "AppUserCredentialsRequestPayload",
        "AppUserProfileRequestPayload",
    }

    model_config = ConfigDict(
        validate_assignment=True,
        protected_namespaces=(),
    )

    def __init__(self, *args, **kwargs) -> None:
        if args:
            if len(args) > 1:
                raise ValueError(
                    "If a position argument is used, only 1 is allowed to set `actual_instance`"
                )
            if kwargs:
                raise ValueError(
                    "If a position argument is used, keyword arguments cannot be used."
                )
            super().__init__(actual_instance=args[0])
        else:
            super().__init__(**kwargs)

    @field_validator("actual_instance")
    def actual_instance_must_validate_oneof(cls, v):
        error_messages = []
        match = 0
        # validate data type: AppUserCredentialsRequestPayload
        if not isinstance(v, AppUserCredentialsRequestPayload):
            error_messages.append(
                f"Error! Input type `{type(v)}` is not `AppUserCredentialsRequestPayload`"
            )
        else:
            match += 1
        # validate data type: AppUserProfileRequestPayload
        if not isinstance(v, AppUserProfileRequestPayload):
            error_messages.append(
                f"Error! Input type `{type(v)}` is not `AppUserProfileRequestPayload`"
            )
        else:
            match += 1
        if match > 1:
            # more than 1 match
            raise ValueError(
                "Multiple matches found when setting `actual_instance` in AppUserUpdateRequest with oneOf schemas: "
                "AppUserCredentialsRequestPayload, AppUserProfileRequestPayload. Details: "
                + ", ".join(error_messages)
            )
        elif match == 0:
            # no match
            raise ValueError(
                "No match found when setting `actual_instance` in AppUserUpdateRequest with oneOf schemas: "
                "AppUserCredentialsRequestPayload, AppUserProfileRequestPayload. Details: "
                + ", ".join(error_messages)
            )
        else:
            return v

    @classmethod
    def from_dict(cls, obj: Union[str, Dict[str, Any]]) -> Self:
        return cls.from_json(json.dumps(obj))

    @classmethod
    def from_json(cls, json_str: str) -> Self:
        """Returns the object represented by the json string"""
        instance = cls.model_construct()
        error_messages = []
        match = 0

        # deserialize data into AppUserCredentialsRequestPayload
        try:
            instance.actual_instance = AppUserCredentialsRequestPayload.from_json(
                json_str
            )
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into AppUserProfileRequestPayload
        try:
            instance.actual_instance = AppUserProfileRequestPayload.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))

        if match > 1:
            # more than 1 match
            raise ValueError(
                "Multiple matches found when deserializing the JSON string into AppUserUpdateRequest with oneOf schemas: "
                "AppUserCredentialsRequestPayload, AppUserProfileRequestPayload. Details: "
                + ", ".join(error_messages)
            )
        elif match == 0:
            # no match
            raise ValueError(
                "No match found when deserializing the JSON string into AppUserUpdateRequest with oneOf schemas: "
                "AppUserCredentialsRequestPayload, AppUserProfileRequestPayload. Details: "
                + ", ".join(error_messages)
            )
        else:
            return instance

    def to_json(self) -> str:
        """Returns the JSON representation of the actual instance"""
        if self.actual_instance is None:
            return "null"

        if hasattr(self.actual_instance, "to_json") and callable(
            self.actual_instance.to_json
        ):
            return self.actual_instance.to_json()
        else:
            return json.dumps(self.actual_instance)

    def to_dict(
        self,
    ) -> Optional[
        Union[
            Dict[str, Any],
            AppUserCredentialsRequestPayload,
            AppUserProfileRequestPayload,
        ]
    ]:
        """Returns the dict representation of the actual instance"""
        if self.actual_instance is None:
            return None

        if hasattr(self.actual_instance, "to_dict") and callable(
            self.actual_instance.to_dict
        ):
            return self.actual_instance.to_dict()
        else:
            # primitive type
            return self.actual_instance

    def to_str(self) -> str:
        """Returns the string representation of the actual instance"""
        return pprint.pformat(self.model_dump())
